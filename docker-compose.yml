version: "3.9"

services:
  app:
    image: python:3.11-slim
    container_name: modular-monolith-skeleton
    working_dir: /app
    volumes:
      - ./:/app
    command: ["python", "-m", "src.pipeline"]

